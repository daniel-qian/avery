# feat-012 · Frozen eval scenario set — status pointer

**Deliverable:** [`docs/strategy/coldstart-deliverables/eval-scenario-set.md`](../../docs/strategy/coldstart-deliverables/eval-scenario-set.md)

**Status:** `in-progress` — design + adversarial flagship DONE; set not yet freezable.

## Done (this session, 2026-06-21)
- Full ~27-row manifest (1 flagship + 10 taxonomy + 8 non-authored REAL + 3 adversarial + 2 null),
  composition on `eval-sheet-spec.md` §1.5.
- **`ADV-01` (Marcus) — the kind-read-is-wrong case — written in full**: drop-in eval-sheet row,
  evidence bundle deliberately inverts Lin Qing (stable scope, unkept PR promises, teammate
  rebuilding his module), Avery names the underperformance + hands a decisive corrective incl. a
  planned dignified exit with **zero number/label on the person**; GPT reject = over-scoring
  (red-line cross), Claude reject = over-softening (calibration fail); symmetry punchline.
- `NULL-01` full; `ADV-02/03` + `NULL-02` sketched.
- Demo beat for feat-013 + freeze/git-hash process + honest handoff.

## Why NOT done
- The ≥3 non-authored `REAL-*` rows are PLACEHOLDER — **await the partner's real materials**.
- The set **cannot be frozen / git-hashed** until those rows carry real material (anti-cherry-pick
  lock, §7 of the deliverable).
- Real preference / red-line-crossing numbers **await the feat-011 harness run**.
- All buyer-facing strings flagged `⚠ 待 Danny 审字`.

## Provenance note
The deliverable file was authored by a background subagent (`claire`/eval-architect) that completed
its write to disk before a host process crash; the content was reviewed and accepted by the main
agent against the locked design (red lines held, adversarial character ≠ Lin Qing).
